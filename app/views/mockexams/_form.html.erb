<%= form_with(model: mockexam, local: true) do |form| %>
  <% if mockexam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mockexam.errors.count, "error") %> prohibited this mockexam from being saved:</h2>

      <ul>
      <% mockexam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :mockexam_name %>
  </div>

  <div class="field">
    <%= form.label :level_id %>
    <%= form.number_field :level_id, id: :mockexam_level_id %>
  </div>

  <div class="field">
    <%= form.label :subject_id %>
    <%= form.number_field :subject_id, id: :mockexam_subject_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
