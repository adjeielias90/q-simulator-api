<%=  simple_form_for(@mockexam, html: { class: 'form-inline' }) do |f| %>
  <% if mockexam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mockexam.errors.count, "error") %> prohibited this mockexam from being saved:</h2>

      <ul>
      <% mockexam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :name, id: :mockexam_name %>
  </div>

  <div class="field">
    <%= f.input :level_id, id: :mockexam_level_id %>
  </div>

  <div class="field">
    <%= f.input :subject_id, id: :mockexam_subject_id %>
  </div>

  <%= f.simple_fields_for :mock_questions do |builder| %>
    <%= render partial: 'mock_question_fields', f: builder %>
  <% end %>
  <%= link_to_add_fields "Add Question", f, :mock_questions %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
